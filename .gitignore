/target/
/.env
/.env.*
!.env.example
*.log
*.tmp

# Local secrets (AI provider keys)
/deepseek.conf
*.key
/github_token.txt
*token*.txt
*.pat

# Local reference checkout of AppFlowy (not part of this project)
/appflowy-src/
/AFFiNE/
/vditor/

# Flutter / Dart build + tooling artifacts
**/build/
**/.dart_tool/
**/.flutter-plugins
**/.flutter-plugins-dependencies
**/.packages

# IDE/editor
.idea/
.vscode/

# OS files
.DS_Store

# Claude Code personal overrides + Playwright MCP screenshots
.claude/settings.local.json
.playwright-mcp/

# production secrets + built web bundle staging
.env.prod
docker-compose.override.yml
clients/mica_flutter/installer/Output/
deploy/web/
dist/
.env
.mcp.json

# Local tooling state
.playwright-cli/
.claude/settings.local.json
.claude/worktrees/

# Local tooling state
.playwright-cli/
.claude/settings.local.json
.claude/worktrees/
.claude/
skills-lock.json

# Agent tooling configs — machine-local; repo keeps only CLAUDE.md (see 9a058de).
# Generated by `npx skills add --agent universal` and assorted AI IDEs.
/.agents/
/.cursor/
/.cursorrules
/.windsurfrules
/.gemini/
/.kiro/
/.qoder/
/.opencode.json
/AGENTS.md
/GEMINI.md
/QODER.md
/.github/*.instruction.md
/.github/copilot-instructions.md

# Local cloud credentials — never commit
aliyun-ak.txt
