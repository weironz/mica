# Mica server environment — copy to `.env.prod` next to the compose file.
# NEVER commit the real file (gitignored).
#
# ONE template for both stacks, with the QUICKSTART as the default: everything
# active below is what a single machine reached by IP needs, and the only lines
# commented out are the two that belong to the domain form.
#
#   quickstart (this file as-is, nginx on :80, reached by IP)
#     docker compose --env-file .env.prod -f docker-compose.single.yml up -d
#
#   behind an existing Traefik (real domain + TLS)
#     docker compose --env-file .env.prod -f docker-compose.yml up -d
#
# ⚠️ THE COMPOSE FILE SELECTS THE FRONT DOOR, NOT THIS FILE.
# Uncommenting DOMAIN does not give you a domain. `docker-compose.single.yml`
# publishes :80 itself and never reads DOMAIN; `docker-compose.yml` carries
# Traefik labels and never reads SERVER_IP. Moving to a domain means switching
# `-f` AND uncommenting those two lines — one without the other does nothing.
#
# Why the defaults sit this way (2026-08-07): they used to be exactly backwards.
# SERVER_IP shipped COMMENTED OUT even though the quickstart refuses to start
# without it, while DOMAIN shipped ACTIVE even though the quickstart never reads
# it — so the majority path failed on a file that looked finished. That, not
# "one file serves two stacks", was the actual defect. Splitting the template in
# two was tried the same day and reverted: it duplicated ~60 lines of identical
# secret/mail/registration commentary, which is the drift this repo keeps paying
# for elsewhere.

# ── QUICKSTART — the address browsers will use ───────────────────────────────
# Presigned S3 URLs embed it, so it has to be reachable FROM THE BROWSER: a
# public IP or a hostname. Not `localhost`, not a docker-internal name.
# docker-compose.single.yml refuses to resolve without it (`${SERVER_IP:?}`).
SERVER_IP=

# ── TRAEFIK FORM — public hostnames; both need DNS A records ─────────────────
# Uncomment these ONLY together with `-f docker-compose.yml` (see the warning
# above). S3_DOMAIN gets its OWN record: presigned S3 URLs embed it, and
# browsers fetch images straight from it. That compose file refuses to resolve
# without both (`${DOMAIN:?}` / `${S3_DOMAIN:?}`).
# DOMAIN=mica.example.com
# S3_DOMAIN=s3.mica.example.com

# ── Image tag (api + web + backup all move together) ─────────────────────────
# Pick a real release: https://github.com/weironz/mica/releases
# Do NOT use `:latest` — the release workflow only pushes `:v<version>`, and the
# `latest` tag on the registry is a leftover from an early manual push
# (2026-06-29) that points at neither current release.
#
# DELIBERATELY EMPTY: a quickstart that silently installs whichever tag happened
# to be newest when this file was written is worse than one that asks. This line
# read `v0.13.6` on 2026-08-05, nine releases behind, and anyone who only edited
# SERVER_IP would have installed that.
#
# BOTH compose files use `${MICA_VERSION:?}`, so an empty value stops the stack
# with an explanation rather than installing something. (Until 2026-08-07 the
# Traefik one fell back to a hardcoded `v0.5.0` instead — silently.)
MICA_VERSION=

# Where to pull from. Defaults to Docker Hub; the maintainer's own server
# overrides it to Aliyun ACR because it runs in CN, where Hub is not dependable.
# MICA_REGISTRY=registry.cn-shenzhen.aliyuncs.com/willspace

# ── App CORS (browser cross-origin reads) ────────────────────────────────────
# Empty/unset = deny all cross-origin (the web app is served same-origin with
# /api, so it needs no grant). Set ONLY if a separate origin must call the API.
# CORS_ALLOWED_ORIGINS=https://other.example.com,https://app2.example.com

# ── rustfs admin console (:9001) ─────────────────────────────────────────────
# ON by default on the quickstart stack, published on :9001. It is a browser UI
# that can browse, download and DELETE the bucket, and it signs in with
# S3_ACCESS_KEY_ID / S3_SECRET_ACCESS_KEY — so at their defaults (above) anyone who read
# this repository can open it. Fine on a LAN; close it or change the keys on
# anything public.
#   RUSTFS_CONSOLE_ENABLE=false        # serve nothing on :9001
#   RUSTFS_CONSOLE_PORT=127.0.0.1:9001 # keep it, but only over an SSH tunnel
# RUSTFS_CONSOLE_ENABLE=
# RUSTFS_CONSOLE_PORT=

# ── Accounts: public self-registration ───────────────────────────────────────
# CLOSED unless you open it here: only an explicit true/1/yes/on opens
# POST /auth/register, and anything else — unset, empty, or a typo — keeps it
# 403ing. Login/refresh for existing accounts are unaffected, and a brand-new
# instance can still create its very FIRST account, so a fresh self-hosted
# install is usable without ever touching this line. Opening it on a public
# node lets strangers mint accounts; they must verify their email to log in.
# MICA_REGISTRATION_ENABLED=true

# ── Password-reset email (see docs/password-reset.md) ────────────────────────
# Default backend "log" writes the reset link to the SERVER LOG and sends nothing
# — the flow still works (read the link from the logs), so leaving this unset is
# a valid "no email yet" state. Set "directmail" + the vars below to send via
# Aliyun DirectMail (邮件推送); missing required vars fall back to "log" with a
# warning, never a crash. MICA_MAIL_FROM must match the verified DirectMail
# sender address exactly. The sender domain is DNS-only (TXT/DKIM) — it needs no
# A record and no Traefik rule; only the reset-LINK domain (below) is inbound.
# MICA_APP_BASE_URL=https://mica.example.com     # origin the emailed link points at; default https://<DOMAIN>
# MICA_MAIL_BACKEND=directmail
# MICA_MAIL_FROM=noreply@mail.example.com        # verified DirectMail sender address (AccountName)
# MICA_MAIL_FROM_NAME=Mica                       # optional display name recipients see
# MICA_MAIL_ACCESS_KEY_ID=
# MICA_MAIL_SECRET_ACCESS_KEY=
# MICA_MAIL_REGION=cn-hangzhou                   # default; rarely changed
# MICA_MAIL_ENDPOINT=https://dm.aliyuncs.com/    # default; only for a regional endpoint

# ── Secrets — every one of them optional, but they are NOT equally safe ──────
# You can start either stack without setting any of these. What that costs
# differs per credential, so read the S3 one before deciding to leave it alone:
#
#   JWT_SECRET         self-minted by the server. No default exists to leak.
#   POSTGRES_PASSWORD  defaulted, and safe: neither compose file publishes a
#                      postgres port, so nothing outside the stack can present
#                      it.
#   S3_ACCESS_KEY_ID /    defaulted, and NOT safe on a public node. rustfs
#   S3_SECRET_ACCESS_KEY      publishes :9000 to the internet on purpose (browsers
#                      presign against it directly), so the default pair — which
#                      is written in this repository — lets anyone who reads it
#                      read and write your files. Fine for a private LAN or a
#                      trial; change it on anything real.

# OPTIONAL since 0.13.16. Leave it blank: the server mints a random key on first
# boot and keeps it in the database (the `jwt_secret` row of `server_secrets`),
# so sessions survive restarts and upgrades and every instance's key is its own.
#
# Set it only to supply your own (e.g. one key shared across a deployment you
# orchestrate yourself). A value you DO set is still held to the strict bar —
# template placeholders, or anything under 32 characters, refuse to start.
JWT_SECRET=

# OPTIONAL. Defaults to `mica`. Set it only if you have exposed the database
# beyond the compose network — publishing a port, attaching another stack, or
# pointing the api at an external Postgres. If you do set it, set it BEFORE the
# first start: the volume keeps whatever password initdb ran with, so changing
# it later locks the api out until you ALTER USER by hand.
# POSTGRES_PASSWORD=

# OPTIONAL, defaulting to `mica` / `mica-default-not-a-secret`, which are
# PUBLISHED IN THIS REPOSITORY. Leaving them alone means anyone who knows that
# constant can read and write this instance's files over :9000. The api warns
# about it in the log on every production start.
#
# THE DEFAULT VALUES, spelled out — you need them to sign in to the rustfs
# console on :9001, and there is nowhere else to look them up:
#   S3_ACCESS_KEY_ID      mica
#   S3_SECRET_ACCESS_KEY      mica-default-not-a-secret
#   POSTGRES_PASSWORD  mica          (only needed for psql / MICA_BACKUP_PGURL)
# They are constants in the compose file, not generated per install. Nothing
# inside the stack asks a human for them — the api and rustfs are wired to each
# other by compose — so they only matter when YOU log in somewhere.
#
# Set both, before the first start, on any instance reachable by people you do
# not trust:
#   openssl rand -hex 12    # S3_ACCESS_KEY_ID
#   openssl rand -hex 32    # S3_SECRET_ACCESS_KEY
# Changing them later also means updating what rustfs already stored — easiest
# on a fresh volume, so decide now rather than after uploading files.
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
